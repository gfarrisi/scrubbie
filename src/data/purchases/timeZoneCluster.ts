const isIntervalBasePurchasedTime = (): boolean => {
return false;
}
const indentifyTimeZoneRange = () => {

}

export const botDetectedTimeZone = async (): Promise<boolean> => {
    //call indentifyTimeZoneRange passing in all purchase times
    //take time zones and check if there is an interval pattern (singal of bot)
return true;
}